version: "3.9"

services:
  web:
    build: .
    container_name: scem_web_EN
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./scem.db:/app/scem.db
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped